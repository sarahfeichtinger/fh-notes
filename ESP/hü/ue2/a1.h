#pragma once

#define WERT1 123456.123456
#define WERT2 456789.456789

// macro for addition of two values or subcalculations
#define ADD(x, y) ((x) + (y))

// macro for subtraction of two values or subcalculations
#define SUB(x, y) ((x) - (y))

// macro for multiplication of two values or subcalculations
#define MUL(x, y) ((x) * (y))

// macro for division of two values or  subcalculations
#define DIV(x, y) ((x) / (y))